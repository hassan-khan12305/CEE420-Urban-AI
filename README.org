# =============================================================================
# File      : README.org -- Student quickstart for the course code
# Author    : Jürgen Hackl <hackl@princeton.edu>
# Time-stamp: <2026-09-11>
# Copyright (c) 2026 Jürgen Hackl <hackl@princeton.edu>
# =============================================================================
#+OPTIONS: toc:nil
#+OPTIONS: num:nil
#+OPTIONS: tags:nil
#+TITLE: CEE420 Urban AI, Course Code
#+SUBTITLE: CEE420 Urban AI
#+AUTHOR: Jürgen Hackl
#+EMAIL: <hackl@princeton.edu>

# -----------------------------------------------------------------------------
#+LATEX_COMPILER: lualatex
#+LATEX_CLASS: princeton
#+LATEX_CLASS_OPTIONS: [11pt]
#+LATEX_HEADER: \version{v0.2}
# =============================================================================

#+latex: \maketitle

Princeton University, Fall 2026. Everything we run in the Wednesday precepts lives here, together with the kits that go with a studio session. Each week adds a folder. You get the new material with one click in GitHub Desktop.

| Folder | What is in it                                                    |
|--------+------------------------------------------------------------------|
| =F00/= | Python foundations, self-study, start here if you have never programmed |
| =P01/= | Getting Started: Python, Maps & Your Coding Agent (Wed Sep 9)   |
| =A01/= | Your Town, Your Agent, Your Map: the first assignment, due Fri Sep 25 |
| =P02/= | The Open City kit: the town's own data, one number per block, a map for the lobby, self-paced from Mon Sep 14 |

Assignment folders such as =A01/= appear beside the precept folders on the day they are released.

The precept notebooks are numbered and meant to be read in order. The first few in each folder are usually what we run together in class, and when a precept runs as a studio instead, as P02 does, the whole folder is a self-paced kit. The rest are self-contained continuations you work through at your own pace, and they explain properly whatever the class used quickly.

If you have never programmed, open =F00/00_01_notebooks_and_kernels.ipynb= first. It takes twenty minutes and it prevents the one confusion that otherwise costs people an evening.

* Before the first precept
Do this at home, not in class. The downloads are large and the campus wifi is not.

1. Follow *[[file:docs/setup-mac.org]]* or *[[file:docs/setup-windows.org]]*. You install VS Code, Docker Desktop, and GitHub Desktop, then clone this repository and open it once in its container.
2. Open =P01/01_01_environment_check.ipynb= and click *Run All*. A map of Princeton should appear.
3. Reply *DONE* in the Canvas pre-flight thread, or tell us what broke. Either answer helps.

No laptop that can run a browser is out of the game: if the local install fights you, use *[[file:docs/setup-codespaces.org]]* and work in the browser instead.

* Working in this repository
Your environment is set up when Princeton appears.

- *Before you edit a notebook, make it yours*: File > Save As, add =-mywork= to the name (=01_02-mywork.ipynb=). Files named =-mywork= are ignored by git, so your work is yours and =git pull= will never overwrite it or refuse to run.
- *Getting each week's code*: open GitHub Desktop, select this repository, click *Pull origin*. New folders appear; nothing you wrote is touched.
- *If a pull ever complains*, see [[file:docs/troubleshooting.org]]. The short version: save your file under a =-mywork= name, then discard changes to the original.

* The three ways to run the code
| Path                    | When                                                              | Guide                                                                    |
|-------------------------+-------------------------------------------------------------------+--------------------------------------------------------------------------|
| VS Code + Dev Container | The normal path, your own laptop                                  | [[file:docs/setup-mac.org][mac]], [[file:docs/setup-windows.org][windows]] |
| GitHub Codespaces       | Docker will not run on your machine, or you are on a borrowed one | [[file:docs/setup-codespaces.org][codespaces]]                            |
| Local Python with uv    | You already manage Python yourself and prefer to                  | =uv sync && uv run jupyter lab=                                          |

The container and the =uv= path install exactly the same packages, pinned in =pyproject.toml= and =uv.lock=. Nothing else is needed, and no notebook in this course should ever ask you to =pip install=.

* Your coding agent
The course agent for the first weeks is Google Antigravity, on its free tier. Setup and sign-in are in [[file:docs/agent-antigravity.org]]. Any agent you already have is welcome too. What the course asks is not which tool you use but that you can say what it did and what you verified.

* Licenses
Course code and notebooks are MIT (see =LICENSE=), so reuse them freely. Data files carry their own terms, listed in each folder's =data/README.org=. OpenStreetMap extracts are ODbL 1.0 and require attribution. The State's boundaries and parcel composite are public records that ask for an acknowledgement of NJOGIS. The Municipality of Princeton's layers are public records under OPRA whose publisher states no licence, which we say plainly rather than assume. Census files are US federal public domain. No file in this repository names a person; owner name, owner mailing address and reporter columns were never requested from the services that publish them, and a parcel's own street location is kept because it names land, not a person.
