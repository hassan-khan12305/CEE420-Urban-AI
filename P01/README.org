# =============================================================================
# File      : README.org -- Precept 01
# Author    : Jürgen Hackl <hackl@princeton.edu>
# Copyright (c) 2026 Jürgen Hackl <hackl@princeton.edu>
# =============================================================================
#+OPTIONS: toc:nil num:nil tags:nil
#+TITLE: Precept 01, Getting Started
#+SUBTITLE: CEE420 Urban AI
#+AUTHOR: Jürgen Hackl

Python, maps, and your coding agent. The notebooks are numbered and meant to be read in order.

The first four are what we run together in class. The rest are self-paced: each one takes something the class used quickly and explains it properly, and none of them is assessed.

| Notebook | What it covers |
|----------+----------------|
| =01_01_environment_check= | One button, and a map of Princeton, to prove your setup works |
| =01_02_a_first_map_of_princeton= | Reading spatial files, what a GeoDataFrame is, drawing layers, a map you can click |
| =01_03_is_this_the_right_princeton= | The three checks that decide whether to believe a dataset, run on one that is lying |
| =01_04_the_walk_radius_duel= | Answering a real question by hand, then delegating it to an agent and auditing the answer |
|----------+----------------|
| =01_05_inside_a_geodataframe= | Rows, columns, categories, and what 95 percent missing names tells you |
| =01_06_coordinate_reference_systems= | Why degrees are not distances, and the same walk measured four ways |
| =01_07_buffers_within_and_intersects= | What a buffer really is, and the one word missing from most reports |
| =01_08_reading_a_map_you_drew= | Improving a plot deliberately, and naming the artefacts that are not bugs |
| =01_09_three_places_three_radii= | Asking the question twelve times, and finding that the threshold does the work |

* The showcase

=showcase_walking_distance.py= is not a notebook and not homework. It is a plain Python script, written by an AI coding agent during the preparation of this course, that answers the Duel question properly, routing along the real network of footways and streets instead of drawing a circle, and reporting how far wrong the circle was. We ran it together at the end of the first precept.

Run it from a terminal inside the container with =python P01/showcase_walking_distance.py=, and it writes a map and a set of layers into =P01/showcase_output/=. Read it afterwards as a specimen of what a capable agent produces from one sentence of instruction. You are not expected to be able to write it in week one. You are expected, by the end of the semester, to be able to read it, doubt it, and check it.

Note that it answers the same question as the Duel, so if you missed the precept, make your own attempt at the Duel first and run the script afterwards.

* Never programmed before?

If you have never programmed, work through the =F00= folder first, or alongside. Nothing in these notebooks assumes more than it covers.

The data in =data/= is pinned so that nothing here depends on a live service. Its sources and licences are listed in =data/README.org=.